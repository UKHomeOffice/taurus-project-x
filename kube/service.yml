---
apiVersion: v1
kind: Service
metadata:
  labels:
    name: taurus-project-x-service
  name: taurus-project-x-service
spec:
  ports:
  - name: http
    port: 80
    targetPort: 80
  selector:
    name: taurus-project-x